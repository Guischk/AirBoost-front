---
---
<section class="py-24 bg-gradient-to-b from-base-100 to-base-200/50">
  <div class="container mx-auto px-4">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-4xl md:text-5xl font-display font-bold text-center mb-4" style="font-family: var(--font-display);">
        The Performance <span class="text-gradient-accent">Gap</span>
      </h2>
      <p class="text-center text-base-content/60 mb-12 max-w-2xl mx-auto">
        Real-world benchmarks comparing direct Airtable API calls vs. Airboost cached queries
      </p>
      
      <div class="card bg-base-100 shadow-2xl overflow-hidden border border-base-content/5 hover:shadow-3xl transition-shadow duration-500">
        <div class="p-8 md:p-12 grid gap-12">
          
          <!-- Airtable Metric -->
          <div class="space-y-3 group">
            <div class="flex justify-between items-end mb-2">
              <div class="flex items-center gap-3">
                <span class="text-lg font-semibold text-base-content/70">Direct Airtable API</span>
                <span class="badge badge-sm badge-error badge-outline font-mono">slow</span>
              </div>
              <div class="flex items-baseline gap-2">
                <span class="text-3xl md:text-4xl font-mono font-bold text-error">~270</span>
                <span class="text-base text-base-content/50 font-mono">ms</span>
              </div>
            </div>
            <div class="h-6 bg-base-200 rounded-full overflow-hidden relative">
              <div class="h-full bg-gradient-to-r from-error to-error/70 w-full rounded-full animate-pulse shadow-lg shadow-error/30"></div>
            </div>
            <div class="flex items-center gap-2 text-sm text-base-content/50">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
              Rate limited to 5 requests per second
            </div>
          </div>

          <!-- Airboost Metric -->
          <div class="space-y-3 group">
            <div class="flex justify-between items-end mb-2">
              <div class="flex items-center gap-3">
                <span class="text-lg font-semibold text-primary flex items-center gap-2">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 group-hover:rotate-12 transition-transform duration-300" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M11.3 1.046A1 1 0 0112 2v5h4a1 1 0 01.82 1.573l-7 10A1 1 0 018 18v-5H4a1 1 0 01-.82-1.573l7-10a1 1 0 011.12-.38z" clip-rule="evenodd" /></svg>
                  With Airboost
                </span>
                <span class="badge badge-sm badge-success font-mono">fast</span>
              </div>
              <div class="flex items-baseline gap-2">
                <span class="text-4xl md:text-5xl font-mono font-bold text-success glow-secondary" style="animation: number-pulse 2s ease-in-out infinite;">1</span>
                <span class="text-base text-base-content/50 font-mono">ms</span>
              </div>
            </div>
            <div class="h-6 bg-base-200 rounded-full overflow-hidden relative">
              <div class="absolute top-0 left-0 h-full bg-gradient-to-r from-success to-secondary w-[1%] rounded-full shadow-lg shadow-success/50" style="animation: bar-glow 2s ease-in-out infinite;"></div>
            </div>
            <div class="flex items-center gap-2 text-sm text-success font-medium">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
              Unlimited requests per second
            </div>
          </div>

        </div>
        
        <div class="bg-gradient-to-r from-base-200/30 via-base-200/50 to-base-200/30 p-6 text-center border-t border-base-content/5">
          <p class="text-base-content/70 text-sm md:text-base">
            Average response time based on production benchmarks.
            <br class="md:hidden" />
            <span class="inline-flex items-center gap-2 font-bold text-base-content mt-1 md:mt-0 md:ml-1">
              <svg class="w-4 h-4 text-success" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path></svg>
              240x faster
            </span>
            for standard queries.
          </p>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  @keyframes number-pulse {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }

  @keyframes bar-glow {
    0%, 100% {
      box-shadow: 0 0 15px oklch(var(--color-success) / 0.5), 0 0 30px oklch(var(--color-success) / 0.3);
    }
    50% {
      box-shadow: 0 0 20px oklch(var(--color-success) / 0.7), 0 0 40px oklch(var(--color-success) / 0.5);
    }
  }
</style>

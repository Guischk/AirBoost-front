---
import { Database, ArrowLeftRight, TrendingUp } from '@lucide/astro';
---
<section id="architecture" class="py-24 bg-gradient-to-b from-base-200/30 to-base-100">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-display font-bold mb-4" style="font-family: var(--font-display);">
        <span class="text-gradient-primary">How it works</span>
      </h2>
      <p class="text-lg text-base-content/60 max-w-2xl mx-auto leading-relaxed">
        Our dual-database architecture ensures your API is always available, even during heavy sync operations.
      </p>
    </div>

    <div class="max-w-4xl mx-auto">
      <div class="relative bg-base-100 rounded-2xl border border-base-content/10 p-8 md:p-12 shadow-2xl overflow-hidden">
        
        <!-- Background Grid -->
        <div class="absolute inset-0 bg-[linear-gradient(rgba(var(--base-content),0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(var(--base-content),0.02)_1px,transparent_1px)] bg-[size:20px_20px] [mask-image:radial-gradient(ellipse_at_center,black,transparent_80%)]"></div>

        <div class="relative z-10 flex flex-col items-center gap-12">
          
          <!-- Top: API Requests -->
          <div class="flex items-center gap-4 w-full justify-center">
             <div class="badge badge-lg badge-ghost gap-2 px-4 py-3 hover:scale-105 transition-transform duration-300 cursor-default">
                <span class="w-2 h-2 rounded-full bg-success animate-pulse"></span>
                <span class="font-semibold">API Requests</span>
             </div>
          </div>

          <!-- Middle: Active DB -->
          <div class="relative group">
            <div class="absolute -inset-1 bg-gradient-to-r from-primary to-secondary rounded-xl blur opacity-25 group-hover:opacity-50 transition duration-1000 group-hover:duration-200 animate-pulse" style="animation-duration: 3s;"></div>
            <div class="relative w-64 h-32 bg-base-100 rounded-xl border border-primary/20 flex flex-col items-center justify-center gap-2 shadow-lg hover:shadow-xl transition-shadow duration-300">
              <Database class="w-10 h-10 text-primary group-hover:scale-110 transition-transform duration-300" stroke-width={1.5} />
              <div class="font-bold font-display" style="font-family: var(--font-display);">Active Database</div>
              <div class="text-xs text-success font-mono font-semibold tracking-wider">SERVING TRAFFIC</div>
            </div>
            
            <!-- Connection Lines -->
            <div class="absolute -top-12 left-1/2 w-0.5 h-12 bg-gradient-to-b from-transparent to-base-content/20"></div>
            <div class="absolute -bottom-12 left-1/2 w-0.5 h-12 bg-gradient-to-b from-base-content/20 to-transparent"></div>
          </div>

          <!-- Atomic Swap Indicator -->
          <div class="group bg-base-200 rounded-full px-6 py-2 text-sm font-mono border border-base-content/10 flex items-center gap-2 hover:border-primary/20 hover:bg-base-200/80 transition-all duration-300 cursor-default">
            <ArrowLeftRight class="w-4 h-4 group-hover:scale-110 transition-transform duration-300" />
            <span class="font-semibold">Atomic Swap</span>
          </div>

          <!-- Bottom: Inactive System -->
          <div class="grid grid-cols-2 gap-8 w-full max-w-lg">
            
            <!-- Inactive DB -->
            <div class="group bg-base-200/50 rounded-xl border border-base-content/5 p-6 flex flex-col items-center gap-2 opacity-60 hover:opacity-80 transition-opacity duration-300">
               <Database class="w-8 h-8 text-base-content/40 group-hover:scale-105 transition-transform duration-300" stroke-width={1.5} />
               <div class="font-medium text-sm">Inactive DB</div>
               <div class="text-[10px] bg-base-300 px-2 py-0.5 rounded font-mono font-semibold">UPDATING</div>
            </div>

            <!-- Airtable -->
            <div class="group bg-base-200/50 rounded-xl border border-base-content/5 p-6 flex flex-col items-center gap-2 relative hover:border-base-content/10 transition-all duration-300">
               <!-- Sync Arrow -->
               <div class="absolute top-1/2 -left-6 transform -translate-y-1/2">
                 <TrendingUp class="w-4 h-4 text-base-content/30 animate-pulse -rotate-45" />
               </div>
               
               <img src="https://upload.wikimedia.org/wikipedia/commons/4/4b/Airtable_Logo.svg" alt="Airtable" class="h-8 opacity-70 grayscale group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-300" />
               <div class="font-medium text-sm">Airtable API</div>
            </div>

          </div>

        </div>
      </div>
    </div>
  </div>
</section>
